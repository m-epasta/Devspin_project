name: "test-project"
description: "Test project for filtering"

commands:
  start:
    dev: "echo 'Starting test project'"
    build: "echo 'Building test project'"

services:
  services:
    - name: "frontend"
      service_type: "web"
      command: "echo 'Starting frontend'"
      dependencies: []
    
    - name: "api"
      service_type: "api" 
      command: "echo 'Starting API'"
      dependencies: ["frontend"]
    
    - name: "database"
      service_type: "database"
      command: "echo 'Starting database'"
      dependencies: []
    
    - name: "cache"
      service_type: "cache"
      command: "echo 'Starting cache'"
      dependencies: ["database"]